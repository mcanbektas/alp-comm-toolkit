import type { ReactNode } from 'react';

import { LiveMonitorScreen } from '../features/live-monitor/LiveMonitorScreen';

export function LiveMonitorPage(): ReactNode {
  return <LiveMonitorScreen />;
}
