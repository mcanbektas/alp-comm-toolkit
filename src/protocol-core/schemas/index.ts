/**
 * Kullanıcı tanımlı protokol şeması — spec §9. Şema VERİDİR: yorumlanır,
 * derlenip çalıştırılmaz (spec §41 `eval` yasağı).
 */
export * from './fieldTypes';
export * from './protocolSchema';
export * from './specFixture';
